INCLUDE_DIRECTORIES(./include)

FILE(GLOB CANNY_SRC_FILES "./src/*.cpp")
ADD_LIBRARY(myCanny STATIC ${CANNY_SRC_FILES})
TARGET_LINK_LIBRARIES(myCanny ${SYS_LIBS_MATLAB_NEEDED} ${MATLAB_LIBRARIES} ${VTK_LIBRARIES})
