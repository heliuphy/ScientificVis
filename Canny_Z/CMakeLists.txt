INCLUDE_DIRECTORIES(./include)

FILE(GLOB CANNY_Z_SRC_FILES "./src/*.cpp")
ADD_LIBRARY(CannyZ STATIC ${CANNY_Z_SRC_FILES})
TARGET_LINK_LIBRARIES(CannyZ ${SYS_LIBS_MATLAB_NEEDED} ${MATLAB_LIBRARIES} ${VTK_LIBRARIES})
