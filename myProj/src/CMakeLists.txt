#AUX_SOURCE_DIRECTORY(. DIR_SRCS)
# 构建静态库
FILE(GLOB SRC_FILES "*.cpp" )
ADD_LIBRARY(myImFill STATIC ${SRC_FILES})
TARGET_LINK_LIBRARIES(myImFill ${SYS_LIBS_MATLAB_NEEDED} ${MATLAB_LIBRARIES} ${VTK_LIBRARIES})
